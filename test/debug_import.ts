import { NotificationPreference } from '../src/modules/notifications/entities/notification-preference.entity';

try {
  console.log('Loaded NotificationPreference:', NotificationPreference);
} catch (error) {
  console.error('Error loading NotificationPreference:', error);
}
