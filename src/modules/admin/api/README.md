# Admin module — API layer

HTTP controllers (none yet).

**Intentional placeholder (2026-07-11):** `AdminModule` is an empty Nest shell and is **not** imported by `AppModule`. No product admin/key-rotation API exists. Do not invent endpoints here — see `docs/stub-module-audit.md`. Key-rotation policy only: `backend/docs/security/key-rotation.md`.
