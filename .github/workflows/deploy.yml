# DISABLED — Production deploys use Coolify only (see RUNBOOK.md).
# This workflow is retained for audit history but cannot execute deploy steps.
name: Backend Deploy

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  blocked:
    name: Legacy SSH deploy disabled
    runs-on: ubuntu-latest
    steps:
      - name: Fail closed
        run: |
          echo "::error::Legacy SSH/PM2 backend deploy is permanently disabled. Use Coolify + GHCR image deploy."
          exit 1
